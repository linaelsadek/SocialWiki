 <p>
    <strong>Commenter:</strong>
    <%= comment.author %>
  </p>

  <p>
    <strong>Comment:</strong>
    <%= comment.text %>
  </p>

  <p>
    <%= pluralize(comment.votes.count, "vote") %>
    <%= button_to 'UpVote', upvote_topic_comment_path(comment.topic, comment), method: :post%>
    <%= button_to 'DownVote', downvote_topic_comment_path(comment.topic, comment), method: :post%>
    <%= button_to 'Delete comment', [comment.topic, comment], method: :delete,
     data: { confirm: 'Are you sure you want to delete this comment?'} %>  
  </p>
